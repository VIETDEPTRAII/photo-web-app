<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <%= link_to 'Your album', root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%= link_to 'Link 1', "#", class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to 'Link 2', "#", class: "nav-link" %>
        </li>
      </ul>

      <% if user_signed_in? %>
        <ul class="navbar-nav">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= fa_icon 'user', text: "#{current_user.email}" %>
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <%= link_to (fa_icon 'edit', text: "Edit profile"), edit_user_registration_path, class: "dropdown-item" %>
              <div class="dropdown-divider"></div>
              <%= link_to (fa_icon 'undo', text: "Log out"), destroy_user_session_path, class: "dropdown-item", method: :delete %>
            </div>
          </li>
        </ul>
      <% else %>
        <%= link_to 'Sign in', user_session_path, class: "btn btn-outline-primary my-2 my-sm-0" %>
      <% end %>
    </div>
  </div>
</nav>
